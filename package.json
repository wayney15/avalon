{
  "name": "avalon-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm --workspace @avalon/web run dev",
    "dev:api": "XDG_CONFIG_HOME=.wrangler-config npm --workspace @avalon/api run dev",
    "db:migrate:local": "XDG_CONFIG_HOME=.wrangler-config npx wrangler d1 execute DB --local --config apps/api/wrangler.toml --file apps/api/migrations/0001_initial.sql && XDG_CONFIG_HOME=.wrangler-config npx wrangler d1 execute DB --local --config apps/api/wrangler.toml --file apps/api/migrations/0002_indexes.sql",
    "build": "npm run build --workspaces",
    "typecheck": "npm run typecheck --workspaces",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "vite": "^5.4.21",
    "vitest": "^1.6.0"
  }
}
